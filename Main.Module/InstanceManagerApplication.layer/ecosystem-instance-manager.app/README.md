# Ecosystem Instance Manager.app

O Platform Main Application e a primeira aplicação a iniciar pela plataforma


O Ecosystem Instance Manager.app ultiliza os mesmo pacotes que o meta-platform-package-executor-command-line utiliza para rodar mais ele é o daemon 
que fornece um api reste para controle de execução
