# web-interface-builder.lib

Constrói/empacota interface web (`.webgui`) com webpack. Fábrica `(SmartRequire) => WebInterfaceBuilder`. Vive no ecosystem-core (capacidade web); consumida por `endpoint-instance` (core) e `desktop-window-instance` (applications, modo gui-host) via injeção do registry.
